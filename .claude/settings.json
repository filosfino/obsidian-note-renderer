{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q 'gh release create'; then echo '[Hook] Release Checklist: 发 release 前请确认已完成 CLAUDE.md 中 Release Checklist 的所有项目（README/CLAUDE.md/TODO.md/CHANGELOG/版本号）。'; fi"
          }
        ]
      }
    ]
  }
}
